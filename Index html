<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>PROVAS CONFIÁVEL LOJINHA</title>
<style>
  :root{
    --bg1:#2b004f; --bg2:#0a0016; --neon-blue:#00aaff; --neon-purple:#8800ff;
  }
  body{
    margin:0; font-family:Arial,Helvetica,sans-serif;
    background: radial-gradient(circle at 30% 20%, #3b005f 0%, var(--bg2) 60%);
    color:#fff; display:flex; align-items:center; justify-content:center; min-height:100vh;
  }
  .card{
    width:min(920px,92%);
    background:linear-gradient(180deg, rgba(20,0,40,0.92), rgba(10,0,20,0.96));
    border:3px solid var(--neon-blue);
    padding:28px; border-radius:12px; text-align:center;
    box-shadow: 0 0 40px rgba(136,0,255,0.12), 0 0 24px rgba(0,170,255,0.08);
  }
  h1{
    margin:0 0 30px;
    font-size:22px; color:#fff; letter-spacing:1px;
    font-weight:700; text-transform:none;
    text-shadow:0 0 12px var(--neon-purple);
  }
  /* botão com luizinha */
  .btn-wrap{ position:relative; display:inline-block; margin:22px auto; }
  .btn {
    position:relative;
    display:inline-block;
    background:#fff; color:#000;
    padding:18px 38px; font-weight:800;
    border-radius:10px; border:4px solid var(--neon-blue);
    font-size:16px; text-decoration:none;
    cursor:pointer; z-index:2;
    transition:transform .12s ease, box-shadow .12s ease;
  }
  .btn:hover{
    transform:translateY(-4px);
    box-shadow:0 14px 40px rgba(0,170,255,0.12);
  }
  /* glow / luizinha */
  .glow {
    position:absolute; left:50%; top:50%;
    transform:translate(-50%,-50%);
    width:calc(100% + 28px); height:calc(100% + 28px);
    border-radius:14px; z-index:1; pointer-events:none;
    background: radial-gradient(60% 60% at 10% 20%, rgba(136,0,255,0.18), transparent 10%),
                radial-gradient(40% 40% at 90% 80%, rgba(0,170,255,0.12), transparent 8%);
    filter: blur(8px); opacity:0.9;
    box-shadow: 0 0 40px var(--neon-blue), 0 0 80px var(--neon-purple);
    animation: pulse 1.6s infinite;
  }
  @keyframes pulse {
    0% { transform:translate(-50%,-50%) scale(0.98); opacity:0.85; }
    50% { transform:translate(-50%,-50%) scale(1.08); opacity:1; }
    100% { transform:translate(-50%,-50%) scale(0.98); opacity:0.85; }
  }
  footer{
    margin-top:30px; color:#cfeaff;
    font-size:12px; opacity:0.9;
  }
  @media(max-width:480px){
    .btn{ padding:14px 22px; font-size:15px; }
    h1{ font-size:18px; }
  }
</style>
</head>
<body>

<div class="card">
  <h1>PROVAS CONFIÁVEL LOJINHA</h1>

  <div class="btn-wrap">
    <a href="https://www.roblox.com/share?code=7d1edd66ecd6f241ad12ebd71954a56a&type=Server" target="_blank" class="btn">PROVAS E VÍDEOS</a>
    <div class="glow"></div>
  </div>

  <footer>© 2025 PROVAS CONFIÁVEL LOJINHA</footer>
</div>

</body>
</html>
